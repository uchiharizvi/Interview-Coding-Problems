package com.interview.coding.companies.datamatics;

import java.util.ArrayList;
import java.util.List;
import java.util.stream.Collectors;

public class Datamatics {
    public static void main(String[] args) {
        //For array of sorted integers, return an array of the squares of each number in sorted order.
        //I/p: [-4,-1,0,3,10]//[-4,-1][0,3,10]//
        //
        //[-7, -4, -3]//[49,16,9]
        //[1, 5, 9]//
        //O/p: [0,1,9,16,100]
        //
        //
        // 2pointers l and r
        //

        //101, James, A, 60000
        //102, Virat, B, 30000
        //103, Rohit, A, 80000
        //104, Serena, A, 90000
        //105, Jason, C, 15000
        //List<Employee> employees = getEmployeeList();
        //List<Employee> employees = new ArrayList<>();
        //O/P
        //List<String> l1 = employees.stream().filter(a->a.getName().equals("A")).collect(Collectors.toList());
        //Using Java Stream get average salary of Employees whose grade is A
        //average
        //employees.stream().filter(a->a.getName().equals("A")).map(b->b.getSalary());




    }
}
